export const pmTaskReload = {
    preserveScroll: true,
    only: ['tasks', 'columns', 'tags', 'notificationPreview'],
};

export const pmTasksOnlyReload = {
    preserveScroll: true,
    only: ['tasks', 'columns'],
};
